from setuptools import setup, find_packages

setup(
    name="myutils",
    version="0.1.0",
    description="A tiny utility package for offline demo",
    packages=find_packages(),
    python_requires=">=3.8",
    # No external dependencies
)
